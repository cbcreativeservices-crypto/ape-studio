/* ============================================================
 *  SITE GATE settings — edit these.
 *  ------------------------------------------------------------
 *  GATE_ENABLED = true  -> visitors must type the key to get in.
 *  GATE_ENABLED = false -> site is fully public (use this at launch).
 *
 *  UNLOCK_KEY   -> the key you hand out. Change it to anything.
 *
 *  (Leave GATE_COOKIE and GATE_TOKEN alone.)
 * ============================================================ */
export const GATE_ENABLED = true;
export const UNLOCK_KEY = "audio2026";

export const GATE_COOKIE = "ape_gate";
export const GATE_TOKEN = "unlocked-ape-2026";
