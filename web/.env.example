# Companion site environment. The anon key is the Supabase PUBLISHABLE key
# (safe to embed in the browser). Never put a service-role key here.
NEXT_PUBLIC_SUPABASE_URL=https://yjgolswjggmlpeowvtxr.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx
# Optional deep link to reopen the mobile app where the user left off.
# Requires a URL scheme registered in the app (app.json "scheme").
NEXT_PUBLIC_APP_DEEPLINK=

# Preview gate (production). Optional — code has fallbacks until these are set.
# GATE_UNLOCK_KEY=
# GATE_COOKIE_TOKEN=
