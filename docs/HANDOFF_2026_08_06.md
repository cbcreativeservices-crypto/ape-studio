# APE Studio — Session Handoff — 2026-08-06

Branch **`audio-tools-engine`**, HEAD **`90cdd7e`**, all pushed, tree clean.
Expo SDK 57 / RN 0.86 / TS-strict. Repo `C:\Users\profe\dev\ape-studio`.

> Full memory + governance backup for this date: `.claude/backups/ape_memory_governance_20260806_155955(.zip)`.

## Hard standing rules
- **Do NOT reference the academic/institutional mode** — the app is commercial-only now until the owner reopens that work.
- Edits/fixes: smallest direct change on the real component; no mockups.
- **Before launch: review/turn off the `src/config/devMode.ts` `DEV_BYPASS` flags** (owner-requested reminder).

## What shipped this session
| Area | Commit | Summary |
|---|---|---|
| Calc Workflows Phase 5 | `ed93a46` | Share-as-image (native pending), YOUR NOTES on results, 1s draft autosave |
| Shared calculator **report** | `7e7b6e7` | Professional report: `features/commercial/brand.ts`, `screens/lab/calc/calcReport.ts`, `ReportCard.tsx`. Company®/type/title/date/PRIMARY RESULT/inputs/results/notes/warnings/footer/report-id. `CalcFunction.primaryResultLabel?`, `Workspace.reportPrefix?` |
| Auth self-heal | `560da79`,`f82180f` | Orphaned session (user_not_found/not_enrolled) no longer bricks — Home + Study fall back to free/public + banner. **Home tab = CourseSelectionScreen, NOT the Dashboard (=Study)** |
| Commercial-first + Landing removal | `f49dab1`,`aed759e` | Boot → login; **WIP pre-auth Landing screen deleted entirely** and unwired |
| Glossary | `b2f9665` | Explore SpinningCount ramps 1→5 digits (was 10,000); "please wait" loading banner restored |
| Entitlement wiring | `b2f9665`,`90cdd7e` | caps = `commercialMode ? capsFor(entitlement) : academy`; reads server `entitlements` on login (academy/lapsed/free); DB migration `grant_authenticated_select_entitlements` |
| Audio Learning | `290089f` | Member Training Labs button reads "EXPLORE THE TRAINING LABS" (free = "PREVIEW TRAINING LABS") |

## Test accounts (LIVE prod `yjgolswjggmlpeowvtxr`)
`pro1@` / `gratis@` / `anorak@` proaudiotrainingacademy.com — password **`278MEDIA!`**.
anorak + pro1 hold active **academy** entitlement (sign in as Academy); gratis = free; guest = anonymous. Old Anorak rebuilt as the graduate (100% of everything).

## Open / owed
- Calc-report **image** share activates only after the next EAS dev build (view-shot + expo-sharing natives). Text share works now.
- **Single-calculator image share** not implemented (text only).
- **Commercial mode itself is incomplete** — Landing removed; build the real pre-auth entry when greenlit.
- On-device validation backlog (wave/meter/digital/mic/speaker sweeps, finer waveform, Light Pulse optical counter — never tested on iOS).
- SPL/OPTIMAL gauge callout positions (`vizMeters` `CALLOUT_POS`) — confirm with owner if still open.

## Dev loop
`npx expo start --dev-client` (loads .env). Pixel USB: `adb reverse tcp:8081 tcp:8081`. Verify: `npx tsc --noEmit -p tsconfig.json` (native RN — not browser-observable). Cloud EAS builds only (no local toolchain on this box).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
