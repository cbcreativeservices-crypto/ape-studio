# Cable Lab Artwork — author flags for owner review (2026-08-16)

Judgment calls and reference-deferred geometry from the prompt authors.
Every FACE image passes the face-geometry verification pass before pin
overlays are enabled, so these flags are review aids, not open risks.

## APE_CABLE_ART_PROMPTS_RECOGNITION.md — 18 prompts
- quarter_patch drawn as the long-frame/B-gauge plug per the task directive, but the record's primary subject is a standard TRS patch plug — the long-frame appears only in notInterchangeableWith ('different tip and contact profile', shape unverified). Prompt defers the tip shape entirely to real B-gauge references; owner should confirm the long-frame depiction is the intended recognition-strip subject vs. a standard TRS patch plug.
- euroblock GREEN body is required per the task directive as the recognized installed-sound convention — the verified record does not state a color (it verifies pitches and screw/spring termination variants only).
- euroblock drawn with exactly 3 terminal positions, grounded in the record's balanced_terminals pinout (+/−/S); the record's channels field is 'varies', so real products span many pole counts.
- edac contact count deliberately unspecified — record verifies 'several contact counts' with facility-specific wiring and pins none; prompt requires a reference-matched dense insert and forbids emphasizing a countable number.
- lk_veam vs socapex_style differentiation: socapex threaded coupling ring and 19 contacts are record-verified; for lk_veam the record says coupling 'varies by series (quarter-turn or threaded)' — I chose quarter-turn plus a dark bulky shell as judgment calls purely to make the two products read as different, per the task directive.
- mini_xlr drawn as the MALE cable connector (pins countable, consistent with the xlr3 side exemplar); the record's typical bodypack/lavalier use would put a FEMALE connector on the cable end. 3-pin variant chosen per directive; 4/5-pin variants noted in the mount line as copy-covered.
- db25 13-pins-over-12 row arrangement is well-known standard D-sub geometry, not stated in the record (record verifies '25-pin D-sub' and screw jackscrews only).
- stage_pin: record verifies flat body, 3 in-line pins, split-pin friction retention; exact pin spacing/offset is NOT in the record, so the prompt defers spacing to E1.24-style product references as instructed.
- nema_twist_lock: no specific NEMA L-configuration is verified in the record (blade patterns are rating-specific by design); prompt specifies a generic 3-blade grounded L-series-style pattern grounded in the record's ac_3c_grounded construction and defers the blade pattern to real references.
- opticalcon_style: record verifies dust caps/sealed shells and push-pull release, not a specific sprung-shutter mechanism; prompt depicts the mating end capped (matches the lab's caps-on inspection teaching) and defers cover geometry to references.
- cam_type rendered neutral matte black with an explicit no-phase-color instruction — judgment call grounded in the record's warning that conductor-color conventions vary by shop/jurisdiction.
- xlr4/xlr5 face-plug prompts added beyond the 1-view tier baseline per task directive (pin count is the identification), bringing the family to 18 prompts instead of 15.

## APE_CABLE_ART_PROMPTS_SCENES.md — 30 prompts
- anatomy__exploded: the exploded part sequence (boot, cable clamp, shell, insert with solder-cup contacts) is composed from lesson02's generic term definitions (strain relief / shell / solder termination) — no record verifies an exploded parts order; body styled generic-XLR-like as the representative connector.
- Cross-sections: conductor insulation colors for balanced_shielded and instrument_unbalanced are NOT record-verified — prompts require two contrasting generic colors and explicitly disclaim any real-world code; speaker_2c conductor differentiation rendered as a molded rib/stripe (lesson says 'marking', but text is prohibited).
- NA color convention (black/white/green) used in xsec__ac_3c_grounded AND in l09__mains_jacket_cut — verified in mains_wall (NEC 200.6/250.119 sourceNote); applying it to the lesson09 vignette (data says only 'colored insulation') is a judgment call.
- l09__ts_crackle: the data describes an audible symptom (crackle on flex); rendered as visible jacket fatigue/cracking/kink at the plug entry so the fault is depictable — judgment call.
- l09__barrel_labels: data requires 'legible, matching labels', which the global NO-TEXT law forbids baking in; prompt specifies smooth BLANK label plates on device and adapter for app overlay.
- l10__xlr_open and l10__ts_intermittent: records verify the fault ('broken conductor or failed joint at one connector' / 'drops on flex') but not WHICH end — break/fray drawn at the right-hand connector as a representative location.
- l10__eth_split: the record verifies the split circuits (pairs 3+6 and 4+5 SPLIT with a perfect pin map); the depicted wrong-partner twisting (white/green bundled with blue, white/blue bundled with green) is the standard physical cause consistent with that map, not itself stated in the record.
- l10 trace colors: XLR/TS/TRS/speakON path tints declared diagram styling only (internal conductor colors unverified); Ethernet trace uses verified T568B pair colors, mains trace uses verified NA convention.
- l10 orientation: first-listed connectorEnds end placed LEFT in every trace (documented in the file) — a diagram convention choice; the master spec's plug-right mating rule governs object renders, not these diagrams.
- l11 layouts: equipment inventories match the lesson11 scene lists exactly, but spatial arrangement (stage vs FOH zones, desk placement) is authored composition — the data specifies no positions.
- Fine geometry deferred to real references per master-spec rule 2: NEMA 5-15 blade positions, combo receptacle face, speakON locked-collar geometry, TOSLINK tip, IEC C13 face — all instructed 'match real product references, authentic not decorative'.
- No T5 prompts in this file: per-core simplified exploded views belong to the family prompt files; anatomy__exploded is the T1b generic that carries the Lesson 2 teaching (noted in the file).

## APE_CABLE_ART_PROMPTS_ANALOG.md — 30 prompts
- T5 naming: master spec section 2 defines no filename for per-core simplified exploded views (only the scene asset anatomy__exploded.png), so this file extends the pattern to <connectorId>__exploded.png — owner should confirm or rename before generating T5 images.
- combo_xlr_trs__face-plug.png is intentionally NOT produced: a receptacle has no plug gender. A reuse note maps the slot to xlr3__face-plug.png (XLR path) and ts_quarter/trs_quarter face-plug assets (1/4-inch path); imageCount excludes it.
- Combo face geometry: prompts describe three female XLR sockets in the standard triangle with the 1/4-inch bore opening at the CENTER of the face (matching the real Neutrik-style parts the record cites); the record's own prose ('XLR contacts in the center, 1/4-inch jack in the surrounding bore') is looser — prompts defer to real product references, which the face-geometry verification pass will confirm.
- Combo depicted LATCHLESS (record verifies latchless combos are widespread, including common compact interfaces; latching models also exist) — judgment call so the art matches the 'check the one in front of you' teaching.
- Combo mating view adaptation: shows BOTH a male XLR and a 1/4-inch TRS plug approaching on diverging axes to teach one-receptacle-two-paths — departs from the standard single-pair mating convention.
- Phone-plug (TS/TRS/TRRS) face-plug views adapted to a steep near-end-on angle: a true dead-on view shows only the tip dome and hides the contact-band count, which is the safety-relevant identification feature. Mount lines state the adaptation.
- Receptacle faces for phone/RCA connectors are shown mounted in a small neutral panel fragment (device-edge fragment for trrs_35) because a bare jack is just a bushing — a presentation judgment consistent with but not stated in the master spec's floating-object rule.
- RCA rendered in neutral finishes with an explicit NO red/white trim requirement: the record verifies red=right/white=left as a labeling convention for stereo pairs, not connector geometry — coloring a single generic connector would imply a channel identity.
- Near-identical receptacle faces: ts_quarter vs trs_quarter panel-jack faces are externally identical (prompts say so and forbid inventing distinguishing features); trs_35 vs trrs_35 are differentiated only by honest context (pro panel jack vs consumer device edge) — judgment call.
- Fine geometry not present in the records (tip retention-groove profile, jack nut/washer shapes, RCA spring-finger count, combo flange/keying clock positions, exploded-view internals) is uniformly delegated to 'match real product references' per master spec section 3 rule 2 — nothing specific was invented.

## APE_CABLE_ART_PROMPTS_SPEAKER.md — 26 prompts
- Layout judgment call: binding posts drawn stacked vertically with red above black in side/face/mating views — the records verify only red=+/black=− (convention) and 19 mm (0.75 in) dual-banana spacing; the vertical red-over-black arrangement is an illustration-consistency choice, not a record fact.
- Filename deviation: bare_wire uses adapted names bare_wire__ferrule.png and bare_wire__strands.png to fill the two face card slots (no faces exist for a non-connector); mapping table is in the file — owner should confirm these names before mechanical integration.
- T5 naming: master spec does not define T5 filenames; used <connectorId>__exploded.png by analogy with anatomy__exploded.png (bare_wire's is a strip-back progression, ts_speaker_legacy's is a barrel-off wiring view).
- Cross-family reuse dependency: ts_speaker_legacy face slots reuse ts_quarter__face-plug.png / ts_quarter__face-recept.png from APE_CABLE_ART_PROMPTS_ANALOG.md — verified ts_quarter is a core analog record, but the analog prompt file must actually deliver those two faces.
- Asset reuse: banana has no face-recept prompt — binding_post__face-recept.png doubles as its receptacle face (record-verified: bananas mate with binding posts in this lab).
- Ungrounded fine geometry deferred to references per spec rule: speakON guide-key positions, locking-collar/latch shape, contact-channel layout, twist-lock ramp geometry, banana spring-leaf count/spacing, binding-post cross-hole placement, ferrule crimp pattern — all instructed as 'match real product references, authentic not decorative' and subject to the face-geometry verification pass.
- Neutral-finish judgment calls: single banana plug body rendered neutral dark (record: a single plug carries no polarity identity); ferrule collar rendered neutral with an explicit no-color-code instruction (real ferrule collars follow a size color code the records do not cover); speakON family rendered all-black (no functional color verified for speakON, unlike powerCON).
- Convention extension flagged: bare_wire__mating shows spring-clip terminals with red/black levers — the record verifies red=+/black=− as general loudspeaker-terminal coloring convention (Pomona-sourced note in binding_post) and lists spring clips as a bare-wire destination, but does not specifically verify spring-clip lever colors.
- speakON NL2 and NL4 side views are deliberately near-identical (true to real parts — same shell family); the 2-vs-4 contact difference reads only in the face views, noted in the file so it is not mistaken for a duplicate-prompt error.
- Dual-banana ground-side tab drawn aligned to the black post per the record's verified convention (tab marks ground/− leg), specified as subtle molded relief only to stay inside the no-legible-text rule.

## APE_CABLE_ART_PROMPTS_POWER.md — 44 prompts
- mains_wall face orientation (blades vertical side-by-side, round-earth feature at 6 o'clock) is a family-level override of the master spec's 12 o'clock keying rule, taken from standard NEMA WD 6 geometry — the verified record confirms blade widths/roles and the round earth pin but not their clock positions; prompts defer exact placement (including which side the wide blade sits on) to real references.
- mains_wall__face-recept is drawn as a SINGLE 5-15R device face (no duplex outlet, no wall plate) in a dark commercial finish — judgment call for canvas clarity and dark-UI fit; owner may prefer ivory or a duplex framing.
- C13/C14 'rectangle with two chamfered corners' outline and 'in-line three-aperture' arrangement are well-known standard geometry not stated in the verified record; prompts state them softly and require matching real product references. Same for the C19 squared-rectangle outline and all C19/C20 contact orientations (record verifies only 'larger, rectangular, differently keyed').
- IEC face orientations (chamfers/keying at 12 o'clock) and the C5 lobe clock arrangement are proposed conventions, not record facts — lock them at the face-geometry verification pass before pin overlays.
- powerCON records verify blue=power-in / gray=power-out and non-cross-mating keying, but NOT which side carries pins vs bushings, nor blue/gray placement on the shell — prompts explicitly forbid guessing contact gender and defer both to real A-type/B-type references.
- Master spec's global style guide mentions 'TRUE1 yellow keying' as functional color, but the verified powercon_true1 record contains no color facts — TRUE1 prompts keep neutral black and allow accents only as real TRUE1-pattern references show them. Owner should reconcile the master-spec mention.
- Filename extensions beyond the master-spec naming list: powercon_xx gray set (__side-gray, __face-plug-gray, __face-recept-gray), iec_c7_c8__face-plug-polarized.png, and per-core T5 __exploded.png names (pattern follows anatomy__exploded.png). Integration registry must include these.
- Scope judgment calls: powerCON gray power-out gets side + both faces but NO mating view (an in/out approach image could visually read as a valid connection, contradicting the record's cannot-cross-mate teaching); TRUE1 power-out variants are noted but not prompted; C7/C8 core views use the common unpolarized form with the verified squared-lobe variant as one optional extra face.
- All T5 exploded prompts are outer-component-only (no terminals, screws, or wiring detail) — deliberate constraint honoring the lab's prohibition on depicting mains termination, slightly narrower than a literal 'exploded view'.
- dc_barrel depicted at generic mid-size with size standards explicitly disclaimed in-prompt (record: sizes vary, no universal electrical standard); jack shown with plain/hex collar per references, friction (non-threaded) form chosen as the common case.
- usb_c_power and poe emit reuse notes mapping to usb_c and ethernet_8p8c image files (digital family) — those filenames assume the digital prompt file uses the canonical <connectorId>__<view>.png scheme.

## APE_CABLE_ART_PROMPTS_DIGITAL.md — 52 prompts
- JUDGMENT: added usb_b__side-usb3.png — the usb_b record teaches 3.x-vs-2.0 mating incompatibility in limitations, commonMistakes, AND notInterchangeableWith, so the taller 3.x silhouette needs its own side view; filename extends the master spec's four-name scheme and needs owner confirmation.
- JUDGMENT: added usb_micro_b__side-usb3.png (wide two-section SuperSpeed plug, taught in notInterchangeableWith) but marked LOWER priority — the record calls the variant rare in audio equipment; it also permits a slight three-quarter angle, deviating from the strict side-view convention so the double-width tip can read.
- NAMING: T5 prompts use <connectorId>__exploded.png — consistent with scene naming style but not explicitly listed in master spec §2; owner should confirm before integration.
- UNGROUNDED GEOMETRY deferred to real references (not invented): which half of the USB-A/B/micro/C opening the tongue occupies and which way contacts face; 8P8C left-to-right pin direction in face views (mirror trap — records verify T568B pair colors/order, not rendered direction); HDMI 19-contact row split/stagger pattern; DIN-5 pin-arc position relative to the keyway; micro-B receptacle internal tongue geometry.
- CONVENTION PICKS (not verified standards): HDMI trapezoid drawn longer-edge-up at 12 o'clock for cross-image consistency; BNC female's two opposed bayonet lugs rendered at 3/9 o'clock (rotationally arbitrary); TOSLINK receptacle rendered flapless (many consumer ports have spring flaps).
- COLOR: informal blue-insulator cue for USB 3.x is NOT record-verified, so the 3.x variant prompts forbid color-coding and rely on shape; only record-verified functional colors required are the Ethernet T568 pair colors (orange/green/blue/brown + white-striped mates) in ethernet_8p8c and its exploded/mating views.
- ADAPTATION: every receptacle in this family is equipment-mounted, so all face-recept and mating prompts stage the receptacle in a small neutral matte-dark panel fragment — an adaptation the cable-to-cable XLR exemplars did not need.
- REUSE MAPPING (note, not prompts): poe → ethernet_8p8c__*.png (plus ethercon_style__*.png for ruggedized contexts); usb_c_power → usb_c__*.png. Both ids live in connectors.power.ts; per the records' teaching the connectors are identical, so no PoE/USB-PD-specific art may be drawn.
- GROUNDING NOTES: 'exactly 19 contacts, two staggered rows' grounded in the hdmi record's pinout+sourceNotes; DIN-5 five pins in a 180° arc with pin 2 central grounded in midi_din5 sourceNotes (pin order 1-4-2-5-3 across the face); BNC two-lug bayonet and 8P8C latch-opposite-contacts treated as well-known standard geometry.
