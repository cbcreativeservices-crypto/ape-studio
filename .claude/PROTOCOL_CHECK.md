PROTOCOL CHECK (read at the START of this response and again BEFORE you deliver it):
1. RE-READ YOUR RULES for any non-trivial task: MEMORY.md, the memory directory, AGENTS.md and CLAUDE.md. Do not act from assumption.
2. FILE PATHS: give the owner full absolute Windows paths as clickable markdown links; use a folder link for a set of files.
3. STEPS: present any actionable/run-list steps as a location-tagged table (Where | What); prefix every command with `cd C:\Users\profe\dev\ape-studio`.
4. BUILD RULE: NEVER run `eas build` / `eas submit` or any billed/external action without the owner's explicit, in-the-moment "go". Ask one line, then WAIT.
5. REPLIES FOR OTHER CHATS/COMPUTERS: paste them IN FULL in the chat, not as a file path.
6. UI WORK: ask to turn on the Browser pane and load the design skills BEFORE building any UI.
7. LOOK FIRST: search docs/, src/, and web/ before asking the owner something the project already answers.
